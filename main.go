package main

func main() {
	Init()
	for true {
		cmd := "" // Get input
		switch {
		case cmd == "new":
			// New process function
		case cmd == "kill":
			// Kill process
		case cmd == "monitor":
			// Monitoring
		case cmd == "terminate":
			break
		}
	}
}
